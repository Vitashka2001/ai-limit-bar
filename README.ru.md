<p align="center"><img src="Resources/AppIcon.png" width="152" alt="Иконка AI Limit Bar"></p>
<h1 align="center">AI Limit Bar</h1>
<p align="center">Лимиты Codex и Claude в строке меню macOS.</p>
<p align="center"><a href="README.md">English</a> · <a href="README.uk.md">Українська</a> · <strong>Русский</strong></p>

AI Limit Bar показывает наиболее актуальный свежий лимит в строке меню, а при открытии — все найденные лимиты Codex и Claude в одной нативной панели.

## Возможности

- автоматически следует за недавней активностью Codex или Claude;
- показывает Codex и Claude рядом, когда оба были активны в течение текущей 30-минутной рабочей сессии;
- всегда отдаёт приоритет свежему критическому лимиту ниже 20%;
- показывает все найденные 5-часовые и недельные квоты;
- позволяет скрыть Codex или Claude одновременно из панели и индикатора строки меню;
- зелёный цвет при 50–100%, жёлтый при 20–49% и красный ниже 20%;
- позволяет переключать Codex и открывает настройки профиля Claude Desktop для смены аккаунта;
- получает актуальные лимиты через официальный интерфейс строки статуса Claude Code;
- кратко запускает Claude Desktop только как резерв, если свежие данные Claude Code недоступны;
- предлагает официальную инструкцию Claude Code и загрузку Desktop, когда источник актуальных лимитов отсутствует;
- явно помечает устаревшие данные Claude;
- английский, украинский и русский языки;
- ручное обновление, пауза мониторинга и автозапуск.

## Источники данных

Данные Codex приходят из локального `codex app-server`. Для Claude основным источником служит официальный интерфейс [`statusLine`](https://code.claude.com/docs/en/statusline) Claude Code. Когда текущий режим Claude Code запускает строку статуса, после ответа она передаёт проценты и время сброса 5-часового и недельного лимитов. AI Limit Bar сохраняет только эти значения и сразу обновляет строку меню.

Один раз включите **Автообновление Claude**, чтобы добавить локальный bridge в `~/.claude/settings.json`. Поля лимитов доступны подписчикам Claude.ai Pro/Max после первого ответа в режиме Claude Code, который запускает `statusLine`. Боковая панель VS Code может её не запускать; тогда резерв через Desktop включается автоматически. Bridge не выполняет сетевых запросов и не читает пароли, OAuth-токены, cookies или API-ключи. Если строка статуса уже настроена, AI Limit Bar не заменяет её.

Если свежие данные Claude Code недоступны, автообновление может ненадолго запустить Claude Desktop без активации и прочитать `plan-usage-history.json`. AI Limit Bar сразу скрывает процесс, возвращает фокус предыдущему приложению, если Electron всё же его запросил, и закрывает только запущенный ею процесс после получения данных или через 30 секунд. Если Desktop уже работает, второго запуска не будет. Если он не установлен, резервный способ просто пропускается. Сторонние IDE-расширения, например Cline, могут использовать отдельный API-биллинг и не передавать лимиты подписки Claude.ai.

## Установка

Требуется macOS 13+, Codex для лимитов Codex и официальный Claude Code для актуальных лимитов Claude либо Claude Desktop как резерв. Скачайте `AI-Limit-Bar-2.4.5.dmg` из [последнего релиза](https://github.com/Vitashka2001/ai-limit-bar/releases/latest), перетащите **AI Limit Bar** в `Applications` и запустите.

Сборка локально подписана, но не нотарифицирована. Если macOS заблокирует первый запуск, нажмите приложение правой кнопкой, выберите **Открыть** и подтвердите.

## Управление

- **Мониторинг лимитов** ставит все фоновые обновления на паузу.
- **Отображаемые сервисы** скрывает Codex или Claude и прекращает опрос этого сервиса. Как минимум один сервис всегда остаётся включённым.
- **Автообновление Claude** предпочитает свежие данные Claude Code, а Desktop использует только как резерв.
- **Сменить аккаунт Codex...** открывает официальный вход Codex.
- **Сменить аккаунт Claude...** открывает настройки профиля Claude Desktop. Без Desktop пункт **Настроить отслеживание Claude...** предложит официальную инструкцию Claude Code и загрузку Desktop.
- **Язык** меняет язык интерфейса и перезапускает приложение.
- **Запускать при входе** управляет автозапуском.
- **Полностью выключить** закрывает приложение.

Для полного отключения снимите **Запускать при входе** и выберите **Полностью выключить**. Для включения снова откройте приложение из `Applications`.

Если источник Claude не настроен, рекомендация автоматически показывается только один раз при первом запуске. Позже её можно открыть вручную через **Настроить отслеживание Claude...**, но обычное обновление само её не показывает.

## Сборка

```sh
swift test
./scripts/build-app.sh
./scripts/package-release.sh
```

Готовое приложение: `dist/AI Limit Bar.app`. Подробнее о данных: [PRIVACY.ru.md](PRIVACY.ru.md).

Логотипы Codex и Claude взяты из [LobeHub Icons](https://github.com/lobehub/lobe-icons), пакет `@lobehub/icons` 5.15.0, лицензия MIT. Копия лицензии находится в `Resources/ProviderIcons/LICENSE`.

AI Limit Bar — независимая open-source утилита, не связанная с OpenAI или Anthropic. Лицензия: [MIT](LICENSE).
